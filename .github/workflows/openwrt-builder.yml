#=================================================
# Description: Build OpenWrt using GitHub Actions
# License: MIT
# Author: kenzo
# Modified: Fixed all syntax errors (especially environment variable spacing)
#=================================================

name: OpenWrt Build with Custom Config

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'ssh'
        required: false
        default: 'false'
      openwrt_version:
        type: choice
        description: 'OpenWrt Version'
        required: true
        default: '23.05'
        options:
          - '23.05'
          - '22.03'
          - 'master'

env:
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_COWTRANSFER: false
  UPLOAD_WETRANSFER: false
  UPLOAD_RELEASE: true
  SSH_PRIVATE_KEY:  $ {{ secrets.SSH_PRIVATE_KEY }}
  PASSWORD:  $ {{ secrets.PASSWORD }}
  REMOTE_HOST:  $ {{ secrets.REMOTE_HOST }}
  REMOTE_PORT:  $ {{ secrets.REMOTE_PORT }}
  DOCKER_ID:  $ {{ secrets.DOCKER_ID }}
  DOCKER_PASSWD:  $ {{ secrets.DOCKER_PASSWD }}  
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id

    name: Build OpenWrt  $ {{ github.event.inputs.openwrt_version }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@main

    - name: Free disk space
      uses: coder-xiaomo/free-disk-space@main
      with:
        tool-cache: false
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: true
        swap-storage: true       

    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install build-essential clang flex bison g++ gawk \
        gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
        python3-setuptools rsync swig unzip zlib1g-dev file wget scdoc \
        llvm python3-pyelftools libpython3-dev aria2 jq qemu-utils ccache rename \
        libelf-dev device-tree-compiler libgmp3-dev libmpc-dev libfuse-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "Asia/Shanghai"
        
    - name: Get current date
      id: date
      run: |
        echo "date= $ (date +'%Y.%m.%d-%H%M')" >>  $ GITHUB_ENV
        echo "date2= $ (date +'%m/%d %Y')" >>  $ GITHUB_ENV
        echo "date3= $ (date +'%m.%d')" >>  $ GITHUB_ENV
        echo "DOCKERTAG= $ {{ secrets.DOCKER_ID }}/openwrt-6p:latest" >>  $ GITHUB_ENV
        VERSION=" $ (echo " $ {{github.event.action}}" | grep -Eo " [0-9.]+" | sed -e 's/ //')" || true
        [ " $ VERSION" ] && echo "VERSION= $ VERSION" >>  $ GITHUB_ENV || echo "VERSION= $ (date +'%m.%d')" >>  $ GITHUB_ENV
        
    - name: Clone source code
      env: 
        REPO_URL: https://github.com/openwrt/openwrt
        REPO_BRANCH: openwrt- $ {{ github.event.inputs.openwrt_version }}
      run: |
        git clone --depth 1  $ REPO_URL -b  $ REPO_BRANCH openwrt

    - name: Free up disk space
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo mkdir -p -m 777 /mnt/openwrt/dl /mnt/openwrt/bin /mnt/openwrt/staging_dir
        ln -sf /mnt/openwrt/dl openwrt/dl
        ln -sf /mnt/openwrt/bin openwrt/bin
        ln -sf /mnt/openwrt/staging_dir openwrt/staging_dir

    - name: Run customize.sh (for IP only)
      run: |
        if [ -f customize.sh ]; then
          echo "✅ Running customize.sh to modify default IP"
          chmod +x customize.sh
          ./customize.sh
        else
          echo "⚠️ Warning: customize.sh not found in project root - skipping IP modification"
        fi

    - name: Update feeds
      working-directory: ./openwrt
      run: |
        echo 'src-git helloworld https://github.com/fw876/helloworld' >> feeds.conf.default
        ./scripts/feeds update -a

    - name: Load custom configuration
      run: |
        if [ -e ./.config ]; then
          echo "✅ Found .config in project root, copying to openwrt/.config"
          cp -f ./.config ./openwrt/.config
        else
          echo "⚠️ Warning: .config not found in project root, using default config"
        fi
        
        if [ -d ./files ]; then
          echo "✅ Found files directory in project root, moving to openwrt/files"
          mv -f ./files ./openwrt/files
        fi
        
        cd openwrt
        ./scripts/feeds install -a
        make defconfig

    - name: SSH connection to Actions
      uses: P3TERX/ssh2actions@v1.0.0
      if: (github.event.inputs.ssh == 'true' && github.event.inputs.ssh != 'false') || contains(github.event.action, 'ssh')
      env:
        TELEGRAM_CHAT_ID:  $ {{ secrets.TELEGRAM_CHAT_ID }}
        TELEGRAM_BOT_TOKEN:  $ {{ secrets.TELEGRAM_BOT_TOKEN }}
        
    - name: Download package
      working-directory: ./openwrt
      run: |
        make download -j $ (( $ (nproc)+1))
        find dl -size -1024c -exec rm -f {} \;
        
    - name: Cache
      uses: stupidloud/cachewrtbuild@main
      with:
        ccache: 'true'
        mixkey: openwrt- $ {{ github.event.inputs.openwrt_version }}
        clean:  $ {{ contains(github.event.action, 'nocache') }}
        prefix:  $ {{ github.workspace }}/openwrt
    
    - name: Compile the firmware
      working-directory: ./openwrt
      run: |
        echo -e " $ (( $ (nproc)+1)) thread compile"
        make -j $ (( $ (nproc)+1)) || make -j1 V=s

    - name: Organize files
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        mkdir firmware
        mv -f openwrt/bin/targets/*/*/{*combined*,*sysupgrade*} ./firmware/ 2>/dev/null || true
        cd firmware
        echo "v $ {{ env.date2 }}" > version.txt
        md5= $ (md5sum *squashfs-sysupgrade* 2>/dev/null | awk '{print  $ 1}' || md5sum *squashfs-combined-efi* 2>/dev/null | awk '{print  $ 1}')
        echo  $ md5 >> version.txt
        rename -v "s/^openwrt/ $ {{ env.VERSION }}-openwrt/" * || true
        echo  $ md5 >> version.txt
        echo "OpenWrt  $ {{ github.event.inputs.openwrt_version }}" >> version.txt
        echo "FIRMWARE= $ PWD" >>  $ GITHUB_ENV
        
    - name: Upload firmware directory
      uses: actions/upload-artifact@main
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_firmware- $ {{ github.event.inputs.openwrt_version }}- $ {{ env.date }}
        path:  $ {{ env.FIRMWARE }}

    - name: Generate release tag
      id: tag
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      run: |
        echo "release_tag= $ (date +"%Y.%m.%d-%H%M")" >>  $ GITHUB_OUTPUT
        touch release.txt
        echo "OpenWrt  $ {{ github.event.inputs.openwrt_version }} Firmware" > release.txt
        echo "Build date:  $ date" >> release.txt
        echo "MD5:  $ md5" >> release.txt
        echo "Firmware includes:" >> release.txt
        echo "- helloworld plugin (if configured in .config)" >> release.txt
        echo "status=success" >>  $ GITHUB_OUTPUT

    - name: Upload firmware to release
      uses: softprops/action-gh-release@v1
      if: steps.tag.outputs.status == 'success' && !cancelled()
      env:
        GITHUB_TOKEN:  $ {{ secrets.GITHUB_TOKEN }}
      with:
        tag_name:  $ {{ steps.tag.outputs.release_tag }}
        body_path: release.txt
        files:  $ {{ env.FIRMWARE }}/*

    - name: Delete workflow runs
      uses: glmghost/delete-workflow-runs@main
      with:
        retain_days: 1
        keep_minimum_runs: 3

    - name: Remove old Releases
      uses: dev-drprasad/delete-older-releases@v0.2.1
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        keep_latest: 3
        delete_tags: true
      env:
        GITHUB_TOKEN:  $ {{ secrets.GITHUB_TOKEN }}
